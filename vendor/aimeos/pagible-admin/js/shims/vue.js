/**
 * @license MIT, https://opensource.org/license/mit
 */

// Re-export the full Vue runtime as a stable entry point so the import map can
// resolve plugins' bare `vue` imports to the host's single Vue instance.
export * from 'vue'
